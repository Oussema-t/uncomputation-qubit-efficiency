# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Benchmark outputs that are regenerated by running the demo.
# The committed figure (qubit_scaling_comparison.png) is a deliverable and is
# tracked deliberately; the raw results JSON is not.
benchmark_results.json

# macOS
.DS_Store

# Generated report intermediates (the PDF itself is committed)
uncomputation_report.html
qaoa_results.json
